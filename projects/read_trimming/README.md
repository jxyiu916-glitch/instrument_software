Read Trimming mini-project

How to run

Run tests:

```bash
pytest projects/read_trimming/tests -q
```

Run the tiny benchmark:

```bash
python3 projects/read_trimming/bench_trim.py
```

Notes

- The project includes a C++ stub in `cpp/` for future performance work and design notes describing how to maintain long-term API stability.
